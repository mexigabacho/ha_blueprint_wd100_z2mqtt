# Copy this file to ha-host.env (gitignored, matches *.env) and fill in your
# own SSH target. Then `source ha-host.env` before running tools/mqtt-watch.sh
# or the example commands in CLAUDE.md that reference $HA_SSH_HOST.
HA_SSH_HOST=user@your-ha-host.local
